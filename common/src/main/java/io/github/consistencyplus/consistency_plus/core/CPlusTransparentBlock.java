package io.github.consistencyplus.consistency_plus.core;

import net.minecraft.block.TransparentBlock;

public class CPlusTransparentBlock extends TransparentBlock {
	public CPlusTransparentBlock(Settings settings) {
		super(settings);
	}
}
